# Terraform (EKS) placeholder

This directory is reserved for Terraform code that will provision AWS EKS for Argo CD workloads.

Suggested future structure:

- `modules/eks/`
- `environments/dev/`
- `environments/prod/`
